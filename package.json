{
  "private": true,
  "scripts": {
    "start": "live-server",
    "deploy": "surge -p ./public -d den-bookstore.surge.sh"
  },
  "devDependencies": {
    "live-server": "^0.8.2",
    "surge": "^0.17.3"
  }
}
